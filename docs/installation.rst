============
Installation
============

At the command line::

    $ easy_install django-signoff

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-signoff
    $ pip install django-signoff
